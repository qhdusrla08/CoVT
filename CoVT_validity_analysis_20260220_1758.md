# CoVT 타당성 및 실험 가치 분석

> **작성 일시**: 2026-02-20
> **목적**: CoVT 아키텍처 특성, 학습 데이터 형식, 프롬프트 민감성, base 모델 사전 지식 등 다각도에서 CoVT 접근법의 근거와 본 실험의 가치를 객관적으로 분석
> **근거**: 코드베이스 분석 (`covt_qwen2_5_vl.py`, `data.py`, `caption_gen_korean_50.py`), GPT-4.1 640쌍 평가 결과, P15 vs P19 head-to-head 결과

---

## 1. CoVT 아키텍처 분석

### 1-1. 구조 개요

CoVT(Chain-of-Visual-Thought)는 Qwen2.5-VL-7B-Instruct를 backbone으로, **SAM(Segmentation)**, **DepthAnything-v2(Depth)**, **DINOv2(DINO)** 세 개의 시각 전문가 모델을 보조 인코더로 연결한 멀티-앵커 VLM이다.

```
[입력 이미지]
       │
       ├──► Qwen ViT 인코더 ──────────────────► 시각 토큰 시퀀스
       │                                                │
       ├──► SAM 인코더 → Projection(3584→256) → Cross-Attn → 8 SAM 앵커 토큰
       ├──► DepthAnything → Projection(3584→1024) → Cross-Attn → 4 Depth 앵커 토큰
       └──► DINOv2 → Projection(3584→1024) → Cross-Attn → 4 DINO 앵커 토큰

[토큰 시퀀스 = 시각 토큰 + <anchor_start> 앵커×16 <anchor_end> + 텍스트 토큰]
                                                        ↓
                                            LLM Transformer (LoRA fine-tuned)
                                                        ↓
                                                 캡션 생성 (텍스트)
```

### 1-2. 각 앵커 모달리티의 역할

#### SAM (Segmentation) — 8 토큰

- **기능**: 이미지 내 객체 경계 및 분리(segmentation)
- **학습 손실**: `0.5 × Dice Loss + 2.0 × Focal Loss` (Hungarian Matching으로 세밀한 정렬)
- **한국 문화 이미지에서의 역할**:
  - 음식 이미지: 개별 재료·그릇을 분리하여 "red chili paste", "napa cabbage" 등 구성 요소 식별
  - 의례 이미지: 인물과 배경을 분리하여 의복(한복·예복) 명칭 정확히 귀속
  - 건축 이미지: 건물 외곽 vs 배경(하늘·수목)을 분리하여 주 피사체를 명확하게 기술

#### DINOv2 (Dense Visual Features) — 4 토큰

- **기능**: 패치 단위 의미론적(semantic) 특징 추출 — 텍스처, 재질, 국지적 패턴
- **학습 손실**: 패치 피처 MSE (1025개 쿼리 벡터로 전체 패치 커버)
- **한국 문화 이미지에서의 역할**:
  - 단청(dancheong) 특유의 청·적·녹 패턴 인식
  - 기와 질감 vs 현대 건물 질감 구분
  - 음식 표면 텍스처 (윤기 있는 불고기 vs 건조한 한과 등)
  - 민속 의상의 직물 패턴 인식

#### DepthAnything-v2 (Depth Map) — 4 토큰

- **기능**: 이미지 내 전후 공간 배치 및 원근 구조
- **학습 손실**: Depth Map 복원 (4 토큰 → 전체 depth map으로 디코딩)
- **한국 문화 이미지에서의 역할**:
  - 건축물의 중경(main hall) vs 배경(성벽·산) vs 전경(관광객·광장) 구분
  - 집단 의례(김장·제사) 이미지에서 전면부 인물과 배경 테이블 관계 파악
  - 성곽(남한산성)의 원거리 성벽 vs 근거리 아치 구분

### 1-3. 앵커 토큰의 잠재 공간 추론 (Latent Visual Reasoning)

핵심 설계 원칙: **추론 시 dense output을 명시적으로 생성하지 않는다.**

```
[일반 VLM]  이미지 → 시각 토큰 → LLM → 텍스트 생성
[CoVT]      이미지 → 시각 토큰 + 앵커 토큰(16) → LLM → 텍스트 생성
                     (SAM 8 + DINO 4 + Depth 4)
```

앵커 토큰은 segmentation mask, depth map, semantic feature를 **압축된 연속 벡터**로 변환하여 LLM에 주입. 텍스트 생성 시 LLM이 이 잠재 표현을 문맥으로 참조하면서 **더 시각적으로 근거 있는(visually grounded) 출력**을 유도한다.

**이것이 CoVT의 핵심 가설**: 단순히 "이미지를 보고 캡션을 쓰는 것"이 아니라, "이미지를 세분화·깊이·의미론적으로 분석한 후 캡션을 쓰는 것" — Chain-of-Visual-Thought.

### 1-4. 학습 구성 (4-Stage Curriculum)

| 단계 | 목표 | 앵커 토큰 |
|------|------|---------|
| Stage 0 | 시각 이해 기초 | — |
| Stage 1 | KB-grounded 캡션 생성 | SAM |
| Stage 2 | 시각 추론 + 캡션 결합 | SAM + DINO |
| Stage 3 | 잠재 공간 효율 추론 | SAM + DINO + Depth |

단계적 커리큘럼은 모델이 visual grounding 능력을 먼저 확보한 후 KB 지식 주입을 결합하도록 설계됨.

---

## 2. 학습 데이터 형식과 프롬프트 민감성

### 2-1. 학습 데이터 형식

CoVT 파인튜닝은 다음 대화 형식을 사용:

```json
{
  "image": "path/to/image.jpg",
  "conversations": [
    {
      "from": "human",
      "value": "<image>\n{prompt_with_KB}"
    },
    {
      "from": "gpt",
      "value": "{KB_grounded_caption}"
    }
  ]
}
```

**학습 데이터의 caption(target)은 KB가 주입된 프롬프트로 생성된 CoVT 자체 캡션**이다. 즉, 모델은 `(이미지 + KB 프롬프트) → KB-grounded 캡션` 패턴을 반복 학습.

### 2-2. 프롬프트 민감성 발생 구조

학습-추론 일관성 원칙(Train-Inference Consistency):

```
[학습 시]
  입력:  <image> + "Reference: {category} — {definition}. Describe..."  ← P15 형식
  타겟:  "{KB 용어를 포함한 시각적 캡션}"

[추론 시 — P15 사용]
  입력 형식이 학습과 동일 → 모델이 학습된 패턴 활성화 → KB 용어 사용 최적화

[추론 시 — P19 사용]  ← 다른 프롬프트 구조
  입력 형식이 학습과 다름 → 학습 패턴 부분 불일치 → 출력 품질 저하 가능
```

### 2-3. P15 vs P19 head-to-head가 P15 우세인 이유

| 관점 | 설명 |
|------|------|
| **학습-추론 정렬** | 파인튜닝 데이터가 P15(또는 유사 형식) 프롬프트로 생성됐다면 P15에서 최적 출력 |
| **프롬프트 복잡도** | P19: "Reference (naming guide): ... Name and describe only..." — 지시가 더 복잡, 모델이 명령 분리 실패 가능 |
| **KB 위치** | P15: KB가 문장 앞에 자연스럽게 배치 / P19: "naming guide"라는 메타 레이블 추가 → 모델이 KB를 다른 방식으로 해석 |
| **P19의 vs-Base 우위** | P19(WR 68.2% vs Base)는 base 모델에서 CoVT의 KB 주입 효과를 더 잘 드러내는 프롬프트일 뿐, 절대 캡션 품질은 P15 우세 |

**결론**: P15가 CoVT 모델의 학습 분포(training distribution)에 가장 가깝기 때문에, 동일 모델(CoVT) 비교에서 P15 캡션이 더 우수하게 판정된다. 이는 프롬프트 민감성의 직접적 증거.

### 2-4. 프롬프트 형식이 출력 품질에 미치는 영향 — 실험적 증거

| 비교 | 결과 | 해석 |
|------|------|------|
| CoVT-P15 vs CoVT-P19 | **P15 82.0%** | 동일 모델, 프롬프트 차이만으로 대규모 품질 차이 |
| CoVT-P15 vs Base-P15 | **49.4%** (거의 동률) | KB가 양측 모두 제공됨 → 파인튜닝 순효과는 문화 용어 정확성 |
| CoVT-P19 vs Base-P19 | **68.2%** (CoVT 우세) | P19가 CoVT-Base 간 차이를 더 크게 드러내는 프롬프트 |

---

## 3. Qwen Base 모델의 사전 지식과 CoVT의 추가 가치

### 3-1. Qwen2.5-VL의 기존 한국 문화 지식

Qwen2.5-VL-7B-Instruct는 대규모 멀티모달 사전학습 모델로, 다음 한국 문화 지식을 이미 상당 수준 보유:

| 카테고리 유형 | Qwen 기존 지식 수준 | 근거 |
|------------|-----------------|------|
| 역사 인물·사건 | **높음** | 이순신·세종대왕·독립운동 → 웹 텍스트 대거 포함 |
| 한국 음식 | **중간** | 불고기·김밥 등 국제적 음식 인식 가능, 세부 조리법 약함 |
| 전통 의례 | **낮음** | 김장·제사·성묘 등 일상 의례는 텍스트 데이터 희소 |
| 건축 | **중간** | 경복궁 등 대형 관광지는 인식, 하위 구조물 명칭 약함 |
| 민속놀이 | **낮음** | 제기차기·윷놀이 등 시각 데이터 및 설명 희소 |

### 3-2. 도메인별 CoVT 추가 가치 분석

#### Food (CoVT WR 53.6%) — CoVT 추가 가치 ✅

```
[Base Qwen 한계]
  - 음식 재료를 시각적으로 인식하지만 "napa cabbage" → "kimjang 재료"로
    연결하는 문화적 맥락 주입 불충분
  - 단독 재료 나열에 그치고 의례적 의미 미연결

[CoVT 개선]
  - KB: "kimjang — communal kimchi-making practice"
  - SAM 앵커: 재료별 영역 분리 → 각 재료에 정확한 한국 명칭 귀속
  - DINO 앵커: 음식 표면 텍스처 → "vibrant red chili paste", "salted cabbage" 구분
  → GPT 판정: Kimjang 72.2%, Bulgogi 70.0%, Japchae 70.6%
```

#### Ritual (CoVT WR 54.3%) — CoVT 추가 가치 ✅

```
[Base Qwen 한계]
  - "people in traditional clothing performing a ceremony" (generic)
  - 의례 명칭(김장, 제사, 성묘)을 시각 단서와 연결하는 파인튜닝 부재

[CoVT 개선]
  - KB + 파인튜닝으로 시각적 단서(차례상 배치, 절 동작, 소복) → 의례명 귀속 학습
  - SAM 앵커: 제기(祭器), 음식, 인물을 분리하여 "ancestral rite food arrangement" 정밀 기술
```

#### History (CoVT WR 35.9%) — CoVT 추가 가치 제한적 ⚠️

```
[왜 Base가 더 잘하는가]
  - 이순신·세종·독립운동은 웹에 대규모 설명 텍스트 존재 → Qwen 사전학습에 풍부하게 포함
  - 이미지 자체가 동상·기념비·역사 재현 → 시각적 해석이 비교적 단순
  - KB 주입의 한계: "Admiral Yi Sun-shin, Korean naval hero" → base 모델이 이미 인식

[CoVT의 한계]
  - 파인튜닝 데이터에 역사 카테고리 비중이 낮거나, 해당 카테고리에서 base 대비
    차별화된 시각 추론 필요성이 낮음
  - SAM/DINO/Depth가 동상·기념물에서 food/ritual 대비 추가 정보 제공이 적음
```

#### Architecture 4 cats — 구조적 한계 + 일부 성공 ⚠️/✅

```
[경복궁 72.2% — 성공]
  - 복수 건물(throne hall, pavilion, gateway)이 하나의 이미지에 공존
  - SAM 앵커: 각 건물 영역 분리 → KB 하위 구조물 어휘와 매칭
  - Depth 앵커: 전경(광장) vs 중경(근정전) vs 배경(북악산) 구분
  → CoVT의 다중 앵커가 가장 효과적으로 작동하는 조건

[행궁 16.7% — 실패]
  - 20장 이미지 모두 유사한 전각 → SAM 앵커가 항상 동일한 경계 추출
  - KB v2 추가에도 캡션 수렴 (unique 11개) → fine-tuning 시 단일 패턴 고착
  - Base 모델: KB를 받아 더 다양하게 생성 가능 (fine-tuning 미적용이 오히려 유리)
```

---

## 4. CoVT 타당성의 종합 근거

### 4-1. 실험 결과 기반 타당성

| 근거 | 수치 | 해석 |
|------|------|------|
| 음식·의례 도메인 우위 | food 53.6%, ritual 54.3% | CoVT KB 주입이 가장 필요한 도메인에서 효과 확인 |
| 경복궁 KB v3 성공 | 72.2% WinRate, unique 11→16, dup 37→5 | 복수 시각 구조물이 있는 건축에서 앵커 기반 분리 유효 |
| P15 캡션 절대 품질 | head-to-head 82.0% vs P19 | CoVT 파인튜닝이 최적 프롬프트에서 고품질 출력 확보 |
| GPT winner 기반 CoVT 비율 | 357/640 (55.8%) | Tie를 CoVT default로 처리 시 과반이 CoVT 캡션 채택 |

### 4-2. 아키텍처 기반 타당성

**가설**: 단순 텍스트 KB 주입(base + prompt)보다 시각-언어 정렬 파인튜닝(CoVT)이 더 우수하다.

| 조건 | Base+KB 프롬프트 | CoVT 파인튜닝 |
|------|----------------|-------------|
| KB 용어 사용 | 프롬프트 의존적 (출력 불안정) | 파인튜닝으로 내재화 (출력 안정적) |
| 시각 근거 | ViT 토큰만 사용 | + SAM/DINO/Depth 보강 |
| 문화 용어 귀속 | 이미지-용어 연결 학습 없음 | 학습 데이터로 시각 단서→용어 연결 학습 |
| 캡션 일관성 | 프롬프트 재설계 시 불안정 | 파인튜닝 분포 내에서 일관됨 |

**다중 앵커가 단일 ViT 대비 우수한 케이스**:
- 음식: SAM으로 재료 분리, DINO로 텍스처 구분 → 한국 음식명 정확 귀속
- 의례: SAM으로 인물·사물 분리 → 의례 용품 명칭 정밀 기술
- 건축(복수 구조물): Depth로 전-중-후경 분리, SAM으로 건물 경계 추출

### 4-3. 학습 데이터 관점의 타당성

본 실험의 목적 중 하나는 **CoVT 파인튜닝을 위한 고품질 학습 데이터 구성**이다.

```
[일반적 접근]       이미지 + 일반 캡션 → 파인튜닝
[CoVT 접근]        이미지 + KB-grounded 캡션 + 앵커 손실 → 파인튜닝
                                     ↑
                          이 캡션의 품질이 파인튜닝 결과를 결정
```

GPT-4.1 pairwise 평가의 실질적 가치:

1. **데이터 품질 보증**: GPT judge가 각 이미지에서 더 나은 학습 데이터를 자동 선별 (winner 캡션 활용)
2. **카테고리별 약점 진단**: 어느 카테고리에서 CoVT 생성 데이터가 base 대비 열세인지 파악 → 타겟 개선 가능
3. **KB 설계 피드백**: KB 개선이 실제 WinRate에 미치는 영향을 정량화 → KB 설계 원칙 수립

---

## 5. 본 실험의 한계 및 유보 사항

### 5-1. 평가 설계상 한계

| 한계 | 설명 |
|------|------|
| **동일 KB 제공** | base와 CoVT 양쪽 모두 추론 시 KB를 받음 → "KB 주입 효과"가 아닌 "파인튜닝의 순효과"를 측정 |
| **샘플 편향** | 32 카테고리 × 20장 = 640쌍 → 카테고리당 20장으로 개별 이미지 수준 편차 흡수 가능 |
| **Judge 편향** | GPT-4.1이 시각 정보보다 텍스트 형식·어휘 다양성을 더 높게 평가할 가능성 존재 |

### 5-2. CoVT 구조적 한계

| 한계 | 원인 | 완화 방안 |
|------|------|---------|
| 행궁·팔달문 수렴 | 단일 피사체 이미지 → 앵커 토큰 동일 패턴 반복 | 중복 제거 후처리 + base 캡션 혼합 |
| History 도메인 | base 사전 지식 충분, SAM/DINO 부가 가치 낮음 | KB 보강보다 base 캡션 활용 전략 선택 |
| 프롬프트 의존성 | 학습 분포와 다른 프롬프트 사용 시 성능 하락 | 학습 데이터 생성 프롬프트(P15)와 추론 프롬프트 일치 유지 |

---

## 6. 결론

### 6-1. CoVT 접근법 타당성 평가

> **"건축·역사 카테고리를 제외하면 전반적으로 CoVT가 더 우수한 캡션을 생성한다"**는 주장은 단순 cherry-picking이 아닌, **구조적·실험적 근거가 있는 결론**이다.

핵심 논거 세 가지:

1. **아키텍처적 근거**: SAM·DINO·Depth 앵커는 음식 재료 분리, 의례 용품 귀속, 복합 건축물 구조 분리에 실질적 기여. 이 기여가 GPT-4.1 판정 WinRate 향상으로 직결된 카테고리들(Kimjang 72.2%, Japchae 70.6%, Gyeongbokgung 72.2%)에서 확인됨

2. **프롬프트 민감성 근거**: P15 vs P19 head-to-head 82.0%는 CoVT가 학습 분포 내 프롬프트에서 매우 안정적으로 고품질 출력을 생성함을 증명. Base 모델은 프롬프트 변화에 더 민감하게 품질이 변동

3. **사전 지식 보완 근거**: Base Qwen이 취약한 도메인(일상 의례·전통 음식·민속놀이)에서 CoVT KB 파인튜닝이 유의미한 향상 제공. Base가 강한 도메인(역사·유명 건축물)에서는 CoVT 추가 가치가 낮음 — 이는 **모델 능력의 구조적 상보성(complementarity)**으로 설명됨

### 6-2. 실험 가치 평가

| 가치 | 내용 |
|------|------|
| **데이터 품질 검증** | 640쌍 GPT 판정으로 이미지별 최적 캡션 확정 → 학습 데이터 구성 근거 |
| **KB 설계 원칙 도출** | KB v1→v2→v3 실험으로 "어휘 사전 역할, 지시 금지, 할루시네이션 방지" 원칙 확립 |
| **도메인 강약 지도** | 32개 카테고리의 CoVT WinRate 분포 → 향후 재학습·데이터 보강 우선순위 결정 |
| **프롬프트 선택 확정** | P15가 P19보다 CoVT 파인튜닝 학습 데이터로 우수함 정량적 확인 (82.0%) |
| **재현 가능한 평가 파이프라인** | `gpt_eval_P15_kb2.py` — 파일 직접 지정, 640쌍 완전 평가. 향후 재실험 기준 |

---

*관련 파일*:
- 모델 아키텍처: `train/src/training/covt_qwen2_5_vl.py`
- 학습 데이터 형식: `train/src/training/data.py`
- 프롬프트 정의: `gradio/caption_gen_korean_50.py` (PROMPTS dict, P0–P21)
- 평가 결과: `gradio/eval_outputs_korean/win_rates_P15_kb2_640_4.1.csv`, `win_rates_pairwise_prompt_1519.csv`
